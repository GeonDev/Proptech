package com.apt.proptech.repository;

import com.apt.proptech.domain.LocatePos;
import org.springframework.data.jpa.repository.JpaRepository;

public interface LocatePosRepository extends JpaRepository<LocatePos, Long> {
}
